[project]
name = "qubrid-mcp-learning"
version = "0.1.0"
description = "Learning MCP with Qubrid API"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.9.0",
    "aiohttp>=3.9.0",
    "python-dotenv>=1.0.0",
]